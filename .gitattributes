*.tpa text
*.bcs text eol=lf
*.bs text eol=lf
*.glsl text eol=lf
/pdb export-ignore
.gitattributes export-ignore
log.MD export-ignore
# readme.md is deliberately NOT export-ignored: `git archive` builds the source
# zip GitHub serves, and the readme opens with "Unzip all files into your IWD2
# folder" -- excluding it made the install instructions the one file guaranteed
# missing from what the player just unzipped.